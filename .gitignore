# Python
.venv/
__pycache__
*.pyc

# Regenerable / large data: vocab lists + embedding matrices + LLM cache (rebuild via embeddings.py)
data/*
!data/lattice_profiles/

# Scratch process logs / staging (not part of the repo)
refine-logs/
review-stage/

.claude
.aris
.agents
.zed
.superpowers
corpora
results
