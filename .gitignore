# Python
.venv/
__pycache__
*.pyc

# Regenerable / large data: vocab lists + embedding matrices + LLM cache (rebuild via embeddings.py)
data/*
!data/lattice_profiles/
!data/lattice_sources/
data/lattice_sources/*
!data/lattice_sources/reference/

# Embedding index sidecars: regenerable from profiles via scripts/build_profile_embindex.py
data/lattice_profiles/*.embindex.npz
data/lattice_profiles/**/*.embindex.npz

# Scratch process logs / staging (not part of the repo)
refine-logs/
review-stage/

.claude
.aris
.agents
.zed
.superpowers
corpora
results
