"""RETIRED: fresh-data residue/splice probe for the reconstructor design decision.

Needs `cloak.reconstruct`, the `survival_by_type` spike, and `roundtrip.roundtrip_batch`
— all deleted from main. The reconstructor track lives on branch
`archive/reconstructor-track`; revive this spike there.
"""
raise SystemExit(
    "retired: needs cloak.reconstruct + roundtrip_batch — see branch "
    "archive/reconstructor-track"
)
