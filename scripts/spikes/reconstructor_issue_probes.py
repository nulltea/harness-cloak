"""RETIRED: CPU-only probes for the reconstructor/frozen-extractor design flaws.

`cloak.reconstruct` (and the rest of the reconstructor track) was archived to branch
`archive/reconstructor-track` and deleted from main; this spike has no live dependency.
Revive both together when the extraction phase starts.
"""
raise SystemExit(
    "retired: needs cloak.reconstruct — see branch archive/reconstructor-track"
)
